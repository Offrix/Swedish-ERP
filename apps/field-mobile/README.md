# Field Mobile

Minimal mobile baseline for thumb-friendly field flows.

## Start

```bash
pnpm --filter @swedish-erp/field-mobile start
```

## Notes

- Uses `packages/ui-core` for the HTML shell.
- Uses `packages/ui-mobile` for the mobile chrome fragment.
