# Desktop Web

Minimal desktop baseline with shared shell and desktop chrome.

## Start

```bash
pnpm --filter @swedish-erp/desktop-web start
```

## Notes

- Uses `packages/ui-core` for the HTML shell.
- Uses `packages/ui-desktop` for the desktop chrome fragment.
